'use client';
import '../../globals.css';
import '../../auth.css';
import { useState } from 'react';
import { Construction, ArrowRight, Eye, EyeOff, ShieldCheck } from 'lucide-react';
import Link from 'next/link';
export default function Login(){ const [show,setShow]=useState(false); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); return <main className="auth-page"><div className="auth-brand"><div className="brand-mark"><Construction size={19}/></div><span>construct<strong>OS</strong></span></div><div className="auth-layout"><section className="auth-pitch"><div className="pitch-content"><span className="pill">THE OPERATING SYSTEM FOR CONSTRUCTION</span><h1>Build better.<br/><i>Run smarter.</i></h1><p>One calm, connected workspace for every project, person, and purchase.</p><div className="trust"><ShieldCheck size={16}/> Trusted by construction teams across Europe</div></div><div className="quote">“ConstructOS gives us a clear view of the entire business — from the site to the office.”<small>— Aleksandar Iliev, Managing Director</small></div></section><section className="auth-card"><div className="auth-heading"><h2>Welcome back</h2><p>Sign in to your ConstructOS workspace</p></div><button className="google"><span>G</span> Continue with Google</button><div className="or"><span>or continue with email</span></div><form onSubmit={e=>e.preventDefault()}><label>Work email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com" required/></label><div className="password-label"><label>Password<div className="password"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label><Link href="#">Forgot password?</Link></div><button className="auth-submit">Sign in <ArrowRight size={17}/></button></form><p className="auth-footer">Don&apos;t have an account? <Link href="#">Create your workspace</Link></p><small className="legal">By continuing, you agree to our Terms of Service and Privacy Policy.</small></section></div></main> }
